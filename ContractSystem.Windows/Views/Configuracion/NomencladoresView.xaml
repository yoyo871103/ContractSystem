<UserControl x:Class="ContractSystem.Windows.Views.Configuracion.NomencladoresView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <TabControl SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}"
                BorderBrush="{StaticResource BrushBorder}" BorderThickness="0">
        <TabItem Header="Unidades de medida">
            <ContentControl Content="{Binding UnidadMedida}"/>
        </TabItem>
        <TabItem Header="Terceros (Clientes/Proveedores)">
            <ContentControl Content="{Binding Terceros}"/>
        </TabItem>
        <TabItem Header="Productos/Servicios">
            <ContentControl Content="{Binding ProductosServicios}"/>
        </TabItem>
        <TabItem Header="Plantillas de Documentos">
            <ContentControl Content="{Binding PlantillasDocumento}"/>
        </TabItem>
    </TabControl>
</UserControl>
