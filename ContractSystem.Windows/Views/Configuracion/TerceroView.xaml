<UserControl x:Class="ContractSystem.Windows.Views.Configuracion.TerceroView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Barra de acciones -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Actualizar" Padding="12,6" Command="{Binding CargarCommand}"/>
            <Button Content="Nuevo" Padding="12,6" Margin="8,0,0,0"
                    Command="{Binding NuevoCommand}"
                    Background="{StaticResource BrushAccent}" Foreground="White" BorderThickness="0"/>
            <Button Content="Editar" Padding="12,6" Margin="8,0,0,0"
                    Command="{Binding EditarCommand}"
                    ToolTip="Editar el tercero seleccionado"/>
            <Button Content="Eliminar" Padding="12,6" Margin="8,0,0,0"
                    Command="{Binding EliminarCommand}"/>
            <Button Content="Reactivar" Padding="12,6" Margin="8,0,0,0"
                    Command="{Binding ReactivarCommand}"
                    ToolTip="Reactivar (solo para eliminados)"/>

            <!-- Separador visual -->
            <Border Width="1" Background="{StaticResource BrushBorder}" Margin="12,2,4,2"/>

            <Button Padding="12,6" Margin="4,0,0,0"
                    Click="BtnDescargarPlantilla_Click"
                    ToolTip="Descargar plantilla CSV para importación">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE896;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                               VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBlock Text="Descargar plantilla CSV" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
            <Button Padding="12,6" Margin="8,0,0,0"
                    Background="#28A745" Foreground="White" BorderThickness="0"
                    Click="BtnImportarCsv_Click"
                    ToolTip="Importar terceros desde archivo CSV"
                    Visibility="{Binding PuedeCrearTercero, Converter={StaticResource BoolToVisibility}}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE8DE;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                               VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBlock Text="Importar CSV" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Filtros y búsqueda -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Filtrar por tipo:" VerticalAlignment="Center" FontSize="12"
                       Foreground="{StaticResource BrushTextInactive}" Margin="0,0,8,0"/>
            <ComboBox x:Name="CmbFiltroTipo" Width="140" Padding="6,4" FontSize="12"
                      SelectionChanged="CmbFiltroTipo_SelectionChanged">
                <ComboBoxItem Content="Todos" IsSelected="True" Tag=""/>
                <ComboBoxItem Content="Clientes" Tag="0"/>
                <ComboBoxItem Content="Proveedores" Tag="1"/>
                <ComboBoxItem Content="Ambos" Tag="2"/>
            </ComboBox>
            <CheckBox Content="Incluir eliminados" IsChecked="{Binding IncludeDeleted, Mode=TwoWay}"
                      VerticalAlignment="Center" Margin="16,0,0,0"
                      ToolTip="Mostrar también los terceros dados de baja"/>

            <!-- Búsqueda -->
            <Border Width="200" Height="28" Margin="16,0,0,0"
                    BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" Background="White"
                    CornerRadius="2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="28"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="&#xE721;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                               Foreground="{StaticResource BrushTextInactive}"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1"
                             Text="{Binding Busqueda, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0" VerticalContentAlignment="Center" FontSize="12"
                             Padding="0,4,8,4"
                             Background="Transparent"/>
                </Grid>
            </Border>
        </StackPanel>

        <!-- Mensaje de error -->
        <TextBlock Grid.Row="2" Text="{Binding MensajeError}" Foreground="{StaticResource BrushError}"
                   FontSize="12" TextWrapping="Wrap" Margin="0,0,0,4"
                   Visibility="{Binding MensajeError, Converter={StaticResource BoolToVisibility}, FallbackValue=Collapsed}"/>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Terceros}"
                  SelectedItem="{Binding Seleccionado, Mode=TwoWay}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  SelectionMode="Single" SelectionUnit="FullRow"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"
                  MinHeight="200"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Binding="{Binding Codigo}" Width="80"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="NIF/CIF" Binding="{Binding NifCif}" Width="100"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="80"/>
                <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="100"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                <DataGridTextColumn Header="Ubic. Expediente" Binding="{Binding UbicacionExpediente}" Width="110"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding IsDeleted, Converter={StaticResource BoolToEstadoUsuario}}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Paginación -->
        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="{Binding InfoPaginacion}" FontSize="12"
                       Foreground="{StaticResource BrushTextInactive}" VerticalAlignment="Center"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="&#xE76B;" FontFamily="Segoe MDL2 Assets" Padding="8,4"
                        Command="{Binding PaginaAnteriorCommand}"
                        ToolTip="Página anterior"/>
                <TextBlock VerticalAlignment="Center" Margin="8,0" FontSize="12">
                    <Run Text="{Binding PaginaActual, Mode=OneWay}"/>
                    <Run Text=" / "/>
                    <Run Text="{Binding TotalPaginas, Mode=OneWay}"/>
                </TextBlock>
                <Button Content="&#xE76C;" FontFamily="Segoe MDL2 Assets" Padding="8,4"
                        Command="{Binding PaginaSiguienteCommand}"
                        ToolTip="Página siguiente"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
