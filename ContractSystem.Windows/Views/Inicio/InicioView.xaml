<UserControl x:Class="ContractSystem.Windows.Views.Inicio.InicioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="500"
             Background="Transparent">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="&#xE80F;" FontFamily="Segoe MDL2 Assets"
                   FontSize="48" Foreground="{StaticResource BrushTextInactive}"
                   HorizontalAlignment="Center" Opacity="0.5"/>
        <TextBlock Text="{Binding MensajeBienvenida}"
                   FontSize="18" Foreground="{StaticResource BrushTextInactive}"
                   HorizontalAlignment="Center" Margin="0,16,0,0"/>
        <TextBlock Text="{Binding MensajeSecundario}"
                   FontSize="12" Foreground="{StaticResource BrushTextInactive}"
                   HorizontalAlignment="Center" Margin="0,8,0,0" Opacity="0.8"/>
    </StackPanel>
</UserControl>
